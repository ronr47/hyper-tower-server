[package]
name = "native_high_performance"
version = "3.0.0"
edition = "2021"

[dependencies]
libc = "0.2"
tokio = { version = "1.38", features = ["rt-multi-thread", "macros", "time"] }

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
